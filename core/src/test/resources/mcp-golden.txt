# Golden MCP response bytes captured from LogPoseMcpHandler at commit aad7650,
# before the McpRpc extraction. Each "### name" line is followed by one exact body.
### initialize.echo
{"jsonrpc":"2.0","id":1,"result":{"protocolVersion":"2024-11-05","capabilities":{"tools":{}},"serverInfo":{"name":"logpose","title":"LogPose — live Android capture","version":"1.6.0"},"instructions":"LogPose exposes the HTTP traffic, push messages, and app events captured from an Android device that is running right now. Start with session_summary or list_events to see what the app did, then get_event for full bodies. When a call failed, get_trace shows what led to it — and when the app never propagated a trace (the usual case), get_related groups the whole flow by a business id such as order_id instead. LogPose can also drive the app: create_mock changes what a request returns, inject_fcm delivers a push that starts a flow, and await_event waits for the result instead of polling — so a check reads mock → inject → await → assert. Those three change what the running app receives; the timeline always shows which rows they produced."}}
### initialize.default
{"jsonrpc":"2.0","id":1,"result":{"protocolVersion":"2025-06-18","capabilities":{"tools":{}},"serverInfo":{"name":"logpose","title":"LogPose — live Android capture","version":"1.6.0"},"instructions":"LogPose exposes the HTTP traffic, push messages, and app events captured from an Android device that is running right now. Start with session_summary or list_events to see what the app did, then get_event for full bodies. When a call failed, get_trace shows what led to it — and when the app never propagated a trace (the usual case), get_related groups the whole flow by a business id such as order_id instead. LogPose can also drive the app: create_mock changes what a request returns, inject_fcm delivers a push that starts a flow, and await_event waits for the result instead of polling — so a check reads mock → inject → await → assert. Those three change what the running app receives; the timeline always shows which rows they produced."}}
### ping
{"jsonrpc":"2.0","id":1,"result":{}}
### tools.call.sync
{"jsonrpc":"2.0","id":1,"result":{"content":[{"type":"text","text":"{\"capture\":{\"running\":true,\"note\":\"Capture is running but nothing has arrived yet.\"},\"total_events\":0,\"sessions\":[],\"by_kind\":{\"http\":0,\"fcm\":0,\"db\":0,\"worker\":0,\"config\":0,\"analytics\":0,\"event\":0},\"failures\":0,\"in_flight\":0,\"duplicate_calls\":0,\"endpoints\":[],\"traces\":[],\"traces_note\":\"No event carries a trace id. LogPose never infers causality: the app opts in by wrapping a flow (LogPose.withTrace { } / traceContext()), which stamps the events it emits — including HTTP rows, when the client is set up with LogPose.traceCalls(...) — so get_trace has nothing to group until then.\"}"}]}}
### tools.call.unknownTool
{"jsonrpc":"2.0","id":1,"result":{"content":[{"type":"text","text":"{\"error\":\"Unknown tool 'no_such_tool'\"}"}]}}
### tools.call.isError
{"jsonrpc":"2.0","id":1,"result":{"content":[{"type":"text","text":"{\"error\":\"Tool 'boom' failed: kaboom\"}"}],"isError":true}}
### error.unknownMethod
{"jsonrpc":"2.0","id":1,"error":{"code":-32601,"message":"Method not found: frobnicate"}}
### error.parse
{"jsonrpc":"2.0","id":null,"error":{"code":-32700,"message":"Parse error"}}
### error.401.hasSessions
{"jsonrpc":"2.0","id":1,"error":{"code":-32001,"message":"Missing or unknown X-LogPose-Token. Copy the current token from the LogPose tool window."}}
### error.401.noSessions
{"jsonrpc":"2.0","id":1,"error":{"code":-32001,"message":"No LogPose capture is registered. Open the LogPose tool window in the project you want to inspect, then retry."}}
### tools.list.count
21
### tools.list.first
{"name":"list_events","description":"List captured events from the running app, newest last. Use this first to see what the app actually did. Returns compact summaries; call get_event for full request/response bodies.","inputSchema":{"type":"object","properties":{"limit":{"type":"integer","description":"Max events to return (default 50)."},"kind":{"type":"string","description":"Filter by kind: 'http', 'fcm', 'db', 'worker', 'config', 'event', or an app-defined kind. session_summary's by_kind lists what this capture holds."},"session":{"type":"integer","description":"Restrict to one app run (see session_summary). Omit for all runs — which mixes them when the app restarted mid-capture."},"method":{"type":"string","description":"HTTP method filter, e.g. 'POST'."},"status_class":{"type":"integer","description":"HTTP status class: 2, 3, 4 or 5."},"contains":{"type":"string","description":"Substring match (case-insensitive) over an event's full searchable text. For HTTP that includes the request and response BODIES, not just the URL — the same body search the plugin's filter bar does; for other kinds it covers the fields the row is built from (db sql/args, FCM data, worker input/output, config values, event sections). Headers are not searched."},"exclude":{"type":"string","description":"Drop events whose text matches this — e.g. 'SFX_GEOFENCE' to hide a chatty location feed."},"failed_only":{"type":"boolean","description":"Only failures: non-2xx responses and errors."},"since_seconds":{"type":"integer","description":"Only events captured in the last N seconds."}},"required":[]}}
### tools.list.last
{"name":"save_scenario","description":"Bottle mock rules into a committable file under .logpose/scenarios. from='rules' saves the rule set as it stands; from='session' walks the capture and builds one replace-rule per endpoint from the LATEST real response, so a recorded session becomes an offline demo. A snapshot never invents data: endpoints with no completed response are skipped and counted, and rows LogPose itself mocked are always skipped rather than laundered back in as 'what the backend said'. Scenario files contain captured response bodies — say so before anyone commits one.","inputSchema":{"type":"object","properties":{"name":{"type":"string","description":"File name, lowercase letters/digits/'-'/'_', ≤ 64 chars."},"from":{"type":"string","description":"'rules' (default) or 'session'."},"note":{"type":"string","description":"Optional one-line note stored with the scenario."},"success_only":{"type":"boolean","description":"from='session' only: keep just 2xx responses. Default false — an error state is very often the thing worth bottling."}},"required":["name","from"]}}
